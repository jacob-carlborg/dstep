dist: trusty
language: d
d:
  - dmd
  - ldc

before_script: git fetch --unshallow
script:
  - "dub --config=test"
  - cd test_package && dub build

os:
  - linux
  - osx

matrix:
  include:
    - d: dmd-beta
      os: linux
    - d: dmd-nightly
      os: linux
    - d: ldc-beta
      os: linux
